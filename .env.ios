# Build natif iOS (`npm run build:ios`) — valeurs PUBLIQUES uniquement.
# Le WebView Capacitor (capacitor://localhost) doit appeler l'API en absolu.
VITE_API_BASE=https://sudoku-coach-gamma.vercel.app
# Clé SDK publique iOS RevenueCat (appl_…) — à remplir après l'étape manuelle 4.0.
VITE_REVENUECAT_IOS_KEY=
