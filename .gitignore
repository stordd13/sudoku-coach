node_modules
dist
.vercel
.env
.env.*
# .env.ios ne contient que des valeurs publiques (URL Vercel, clé SDK publique) :
# versionné pour que le build iOS soit reproductible depuis un clone frais.
!.env.ios

# iOS (Capacitor) : fichiers générés par `npx cap sync ios` ou Xcode
ios/App/build
ios/App/Pods
ios/App/output
ios/App/App/public
ios/App/App/capacitor.config.json
ios/DerivedData
**/xcuserdata
